<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:windowBackground">@drawable/app_background</item>-->
    </style>

    <style name="FontStyleDusha" parent="@android:style/TextAppearance.Small">
    <item name="fontFamily">@font/dusha</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <style name="MyTheme" parent="Theme.AppCompat.Light" >
        <item name="android:statusBarColor">@color/colorBlue_text</item>
        <item name="colorAccent">@color/colorBlue_text</item>
    </style>

</resources>
